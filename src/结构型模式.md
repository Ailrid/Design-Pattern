# 结构型设计模式

结构型模式主要包含两部分，一部分是多个对象之间的结构，表现为研究如何组织对象构建更复杂数据结构；一部分是对象内部的结构，表现为研究如何修改对象的内部结构从而增加新的功能。

## 1: Adapter

### 一句话总结

- **本质：**将一个类型当作另一个类型来使用。

- **优点：**在不更改其他源代码的前提下，复用其他类型的功能。

### OOP写法

在OOP里分为类适配器与对象适配器。

- 类适配器：通过多重继承，即A继承B的同时，也继承一个类C，实例化A并调用C声明的方法。这种方法往往由于多重继承导致的问题太大，甚至有的语言直接不支持多重继承，因此不建议使用。
- 对象适配器（本质是组合）：C内部直接持有一个B的引用，直接把对C的调用转发给B。

### 现代写法

在Rust中，根本不需要这种又臭又长的写法。**孤儿规则**确保了，**只要trait或struct中有一个是在当前 crate中定义的**，你就可以直接为这个类型实现这个Trait。

因此你可以直接在不改动源代码的前提下复用源代码并当作另一个类型来使用。

```rust
// 第三方库的结构体，你没法改它的源码
pub struct ThirdPartyLogger; 
impl ThirdPartyLogger {
    pub fn log_to_file(&self, msg: &str) { /* ... */ }
}
// ==================== 自己的代码 ====================
// 你自己系统定义的标准接口
pub trait AppLogger {
    fn write_log(&self, text: &str);
}

// Rust 允许你直接为第三方的结构体实现你自己的 Trait！
impl AppLogger for ThirdPartyLogger {
    fn write_log(&self, text: &str) {
        self.log_to_file(text); // 直接转发
    }
}
```

但是，如果trait和struct都不是自己定义的，为了绕过孤儿规则，就需要用一个本地的结构体把它包起来，这就是Newtype模式。

```rust
// 必须写一个本地结构体来绕过孤儿规则
struct MyLoggerAdapter(ThirdPartyLogger);

impl AppLoggerFromOtherCrate for MyLoggerAdapter {
    fn write_log(&self, text: &str) {
        self.0.log_to_file(text);
    }
}
```

### 回顾思考

现代语言里，Adapter中的类适配器基本已经没啥用，对象适配器也往往成了一种基本的技巧（甚至都不需要教）。

## 2: Bridge

### 一句话总结

- **本质：**把类型的定义和实现解耦，并允许其独立演化。

- **优点：**运行时可以动态的更换实现而不用修改源代码，避免类继承爆炸。（本质上还是一种组合思想）。

### OOP写法

定义两个**独立的类层次结构**：

1. **抽象化角色（A）**：定义给外部用的高层接口（是一个类或抽象类，派生出 A1, A2...）。
2. **实现化角色（B）**：定义底层的、真正的操作接口（是一个接口或抽象类，派生出 B1, B2...）。

**“桥”的建立**：A 内部持有 B 的引用。外部调用 A 的方法时，A 内部将其转发给 B 的方法。

### 现代写法

在Rust中，也不需要搞这种两个抽象，通过泛型，可以直接实现桥接模式的核心功能而且也没有OOP的动态开销。

```rust
// ==================== 底层实现 ====================
trait Decoder {
    fn decode_frame(&self);
}

struct WindowsDecoder;
impl Decoder for WindowsDecoder {
    fn decode_frame(&self) { println!("Windows 底层硬解"); }
}

struct LinuxDecoder;
impl Decoder for LinuxDecoder {
    fn decode_frame(&self) { println!("Linux 底层软解"); }
}

// ==================== 外部接口抽象 ====================
// 这里的泛型 T 就是那座“桥”
struct VideoPlayer<D: Decoder> {
    decoder: D, // 组合驱动
}

// 业务层面的扩展（独立于驱动变化）
impl<D: Decoder> VideoPlayer<D> {
    fn play_vcd(&self) {
        // 上层业务逻辑
        self.decoder.decode_frame();
    }
    
    fn play_blue_ray(&self) {
        // 另一个上层业务逻辑
        self.decoder.decode_frame();
    }
}
```

### 回顾思考

桥接模式和适配器模式一样，在现代语言里沦为了语言的基本功能之一，不再需要OOP的笨拙写法。

## 3: Composite

### 一句话总结

- **本质：**统一整体与部分的关系，使得组合而来的复合对象和单个对象，在行为和类型上看起来相同。

- **优点：**方便树形结构的遍历、描述和添加修改，如UI树，抽象语法树。

### OOP写法

组合模式在OOP中非常常见，**凡是出现树形结构，基本都逃不了组合模式。**

在传统 OOP（如 TS）中，它的写法是：

```typescript
// 统一的语义接口（开放多态）
interface UIComponent {
    render(): void;
}

// 叶子节点（原子语义）
class Button implements UIComponent {
    render() { console.log("  渲染一个按钮"); }
}

// 组合节点（容器，既是 UIComponent）
class Panel implements UIComponent {
    private children: UIComponent[] = [];

    public add(child: UIComponent) { this.children.push(child); }

    render() {
        console.log("渲染一个面板，开始向下递归：");
        // 核心：一视同仁，客户端和父节点都不需要写 switch/case 或者是 match
        this.children.forEach(child => child.render());
    }
}
```

关键在于，基本的叶子节点和容器节点，要继承同一个父类，在容器节点内，利用多态将其他的叶子节点或者新的容器节点与父容器连接。

### 现代写法

在Rust中没有OOP，但是利用代数类型和`trait`与模式匹配，一样可以实现这种树形结构的描述。

正如论UI中的组合与继承中写的一样，这种方式相比OOP有一种致命的缺陷：**无法在不更改源代码的前提下，添加新的节点类型。**

这种方式非常优雅且安全，但更适合描述AST之类的固定种类的抽象语法树，对于需要动态扩展的UI树就很困难了。

```rust
// 用代数类型穷尽节点的所有可能性（闭合多态）
#[derive(Debug)]
pub enum UIWidget {
    Button { text: String },
    Text { content: String },
    Panel { children: Vec<UIWidget> },
}

impl UIWidget {
    // 统一的行为方法，内部通过模式匹配（Pattern Matching）分发逻辑
    pub fn draw(&self) {
        match self {
            UIWidget::Button { text } => {
                println!("  [渲染按钮]: {}", text);
            }
            UIWidget::Text { content } => {
                println!("  [渲染文本]: {}", content);
            }
            UIWidget::Panel { children } => {
                println!("[开始渲染面板] ->");
                // 一视同仁地向下递归遍历
                for child in children {
                    child.draw();
                }
                println!("<- [面板渲染结束]");
            }
        }
    }
}
```

或者，可以强行模拟OOP的开放多态的动态分发，但是这实际上又退化为了OOP中的虚表分发了。

```rust
// 开放多态的接口
trait Widget {
    fn draw(&self);
}

// 组合节点：不再持有具体类型，而是通过 Box<dyn Widget> 强行开辟开放多态的后门
struct Container {
    children: Vec<Box<dyn Widget>>, // 类型擦除，通过虚表动态分发
}

impl Widget for Container {
    fn draw(&self) {
        for child in &self.children {
            child.draw(); // 递归调用
        }
    }
}
```

### 回顾思考

组合模式主要用来描述任何具有树形结构的实体，可能是文档、UI、抽象语法树，等等等等。

在OOP中主要利用继承来实现这种多态，在非OOP语言中需要代数数据类型才能优雅的实现对树形结构的描述。

##  4: Decorator

### 一句话总结

- **本质：**在不修改源代码的情况下，为对象赋予新的功能，并维持原来的类型。

- **优点：**不需要修改原来对象的源代码，实现可拆卸和可选择的新方法添加。

###  OOP写法

传统的 OOP 方式是利用**类型套娃**：利用继承，让包装类（Decorator）和被包装类（Component）同时继承同一个基类（或实现同一接口）来保证它们可以被当作同一类型使用。包装类内部持有基类的引用，在转发原有方法的基础上，动态地增加新的行为。

然而在现代带有元编程特性的高级语言中，Decorator（装饰器）已经成为了一种语言级别的基础设施，例如 TS、Java（注解）和 Python 中的 `@Decorator` 语法。

### 现代写法

在 Rust 这种没有传统继承、追求零开销的语言里，装饰者模式进化成了两种截然不同的现代形态。

**方案 A：泛型Trait**

Rust 的标准库（特别是 `Iterator` 和 `Read/Write`）是装饰者模式的终极进化版。它不需要运行时指针跳转，而是利用**泛型trait**在编译期把套娃彻底拍扁。

```rust
use std::io::{self, Read};

// 被包装的底层组件
struct FileStream;
impl Read for FileStream {
    fn read(&mut self, buf: &mut [u8]) -> io::Result<usize> {
        println!("从文件中读取原始数据...");
        Ok(buf.len())
    }
}

// 现代装饰器：利用泛型 R 约束被包装对象，同样实现 Read Trait
struct BufferedDecoder<R: Read> {
    inner: R, // 静态组合
}

impl<R: Read> Read for BufferedDecoder<R> {
    fn read(&mut self, buf: &mut [u8]) -> io::Result<usize> {
        // 1. 调用被包装对象的方法
        let amt = self.inner.read(buf)?;
        // 2. 施加新的装饰功能（比如解密或缓存）
        println!("--- [现代装饰器] 自动对读取的数据进行解密 ---");
        Ok(amt)
    }
}

fn main() {
    let raw_stream = FileStream;
    // 静态套娃：编译期类型固化为 BufferedDecoder<FileStream>
    let mut decorated_stream = BufferedDecoder { inner: raw_stream };
    
    let mut buf = [0; 10];
    let _ = decorated_stream.read(&mut buf);
}
```

**方案 B：编译期宏**

如果所谓的“装饰”只是为了增加功能（如加日志、加权限校验、加性能耗时统计），Rust直接使用过程宏在编译期改写 AST，连结构体套娃都省了。

```rust
// 伪代码：利用过程宏装饰函数
#[log_execution_time] // 这个宏会在编译期重写该函数，在前后插入统计时间的代码
fn fetch_data() {
    // .....
}
    // 核心业务逻辑
```

### 回顾思考

装饰器在OOP里很有用，但是现在基本上已经变成了语言基础设施，不需要用笨拙的套娃方法。而在现代语言里，装饰器已经被直接内置，或被更高级的功能代替了。

## 5: Facade

### 一句话总结

- **本质：**就是封装一个高层函数（或高层类），把里面一堆复杂的子系统调用藏起来。

- **优点：**隐藏内部实现。

### 回顾思考

这个设计模式有凑数嫌疑，有点废话，说白了就是程序员天天写的函数封装。

## 6: Flyweight

### 一句话总结

- **本质：**提取出大量重复对象里的共同部分（共享），把独有的部分（不共享）动态传进来，以此省内存。

- **优点：**节省内存，避免重复创建大量相同对象。

### 回顾思考

原书之所以长篇大论这么明显的设计，是因为当年 OOP 强行要求“万物皆对象”，导致属性和逻辑必须死死绑在对象上。 比如在一款战争游戏里，屏幕上有 10 万棵树。如果每棵树都是一个独立的类实例，内存直接当场卸载。因此解法是：搞一个 `TreeModel`（共享的纹理），然后把 `X, Y`（外部状态）作为参数在 `draw(x, y)` 的时候传进去。

在现代开发（尤其是Rust这种偏向数据驱动的语言，或者游戏开发的ECS架构）中，`Flyweight`模式彻底失去了独立存在的价值。

因为显然，**它就是局部退化的、手写版的 ECS 组件。**

## 7: Proxy

### 一句话总结

- **本质：**控制对原对象的访问，在不改变原对象接口的前提下，在访问前后加入额外的控制逻辑。

- **优点：** 将核心业务逻辑与副作用（如缓存、权限、懒加载）解耦。

### OOP写法

原始方法其实和Decorator差不了很多，只是由添加新方法变成了转发原有方法的时候加入副作用。

现代语言都直接在语言层面内置了代理机制，让这个模式的存在意义也消失了。比如TS里老生常谈的`Proxy`。

```ts
const realUser = { name: "Alice", role: "user" };

const rbacProxy = new Proxy(realUser, {
    get(target, prop) {
        if (prop === 'deleteDatabase' && target.role !== 'admin') {
            throw new Error("权限不足！");
        }
        return Reflect.get(target, prop);
    }
});
```
### 现代写法

和装饰器一样，Rust直接使用宏来解决，编译期直接把函数生吞进去，改写它的抽象语法树，然后再吐出一个被代理逻辑包裹后的新函数。甚至不需要运行时开销。

### 回顾思考

代理是非常重要的功能，因此现代语言几乎全部内置甚至直接用更高级的方法解决了。

## 补充: Hooks到底是什么？

也许你到这里回想，前端经常使用的`Hooks`，到底对应的是什么呢？从外在表现上讲，`Hooks`其实有些类似于`Decorator`。他们的最大目的都是为了在不影响其他部分的前提下，为对象/组件加入新的功能。

**但是，这只是表象，二者完全不是一个领域的东西。**

- **结构型模式的本质：** 是OOP的产物。他研究的是如何构建复杂数据结构和如何为结构添加新功能。能够使用的前提是，必须存在一个真正的拥有数据的实体。

- **Hooks 的本质：** 是函数式编程的产物。它不是结构型模式，因为使用`Hooks`的前提并不要求必须存在某些实体，他也做到了为组件添加新功能。

实际上，在函数式流行之前，在`Hooks`出现之前，前端为了给组件加入新功能，用的就是标准的结构型模式（装饰器/代理）。

```ts
// 这就是地地道道的面向对象装饰器模式
const EnhancedComponent = withAuth(withLogger(MyComponent));
```

实际上，二者的根源在于OOP的老生常谈的问题：**OOP把结构与功能死死绑定在了一起。**

- 第一个问题是，当你使用继承来实现多态时会捆绑一堆额外的大礼包赠送给你。
- 第二个问题是，当你想要增加新的功能，必须得先持有类和对象。

不过，我们在这里主要说的是后者，关于“如何为结构添加新功能”的部分，在前者“如何构建复杂数据结构”上，前端正是非常好的践行了组合设计模式（虽然如同我们讨论的，**它名义上叫**组合，在本质上却是依靠多态实现的）

## 结论：创建型与结构型设计模型的消失

回顾以上的所有结构型模式，可以发现，这些所有设计模式，在现代几乎已经完全成为了语言的内置特性，或者被更高级的泛型与宏代替了，这是为什么？

整个编程语言其核心就是研究两件事，组织数据结构与操作控制流，后者属于行为型设计模式的核心内容。

而前者，则又包含了两个部分：结构的创建与结构的组成本身。这正好对应了创建型设计模式和结构型设计模型的核心内容。

相比于操控动态、变化的控制流，研究静态、固定的数据结构显然要更简单，也更容易内置于语言本身。

因此，可见创建型设计模式和结构性设计模式的大部分内容都成为了现代语言的内置特性不是没有原因的。这 12 种设计模式（创建型 5 种 + 结构型 7 种）在今天看来，很大程度上是因为 1994 年的 C++/Java 缺乏现代高级泛型、缺乏元编程（宏/装饰器语法）、缺乏代数数据类型，且强制万物皆对象所导致的工程妥协，在现代编程语言面前自然就消失了。

